# Chronicler Log

Append-only operation log.

- 2026-04-25 — Added Kdenlive 25.x serialization knowledge pages: shape, uuid trap, twin chain, per-track tractor, source pointers, golden-fixture testing. First v25-compatible MCP output opened cleanly in Kdenlive 25.08.3.
- 2026-04-25 — Multi-track + editable title smoke (`smoke_2`) opens in Kdenlive. Six contract fixes: main sequence needs `kdenlive:control_uuid`, no `hide` on the sequence's black-track ref, every timeline `<entry>` carries `<property name="kdenlive:id">`, `kdenlive:kextractor=1` on both timeline and bin twins, bin twin uses `mlt_service=avformat` (timeline keeps `-novalidate`), user clip `kdenlive:id` starts at 4 (2 = Sequences folder, 3 = sequence). Title cards must be `mlt_service=kdenlivetitle` with `xmldata` payload, `clip_type=2`, and a host-installed font. Added [[kdenlive-title-card-pattern]] page.
- 2026-04-25 — Cross-dissolve smoke (`smoke_3`) opens in Kdenlive. Same-track dissolves are NOT supported at the XML level (`transitionsrepository.cpp:113-115` returns "Disabled" set used only for UI labelling); Kdenlive always saves dissolves as the stacked-clips pattern. Added `SequenceTransition` model + serializer wiring. Output uses `mlt_service=luma` + `kdenlive_id=dissolve` inside the main sequence tractor, `a_track`/`b_track` = 1-based ordinals into the sequence's `<track>` list with index 0 = black_track. Captured as [[kdenlive-cross-dissolve-pattern]].
- 2026-04-25 — Smoke batch 4 added: outputs 005-back-to-back-cuts, 006-trimmed-clip, 007-multi-title-cards, 008-audio-music-bed, 009-chapter-markers, 010-three-clip-dissolves, 011-wipe-transition. All 11 smoke `.kdenlive` files now sit in `Video Production/tests/mcp_output/` for visual review with sortable `NNN-` prefixes.
- 2026-04-25 — Dissolve direction fix: ``a_track < b_track`` is enforced by Kdenlive; encode direction via the ``reverse`` property, not by swapping track ordinals. Smoke 010 had ``a_track > b_track`` for the V2→V1 dissolve and triggered "Incorrect composition ... was set to forced track" -- fixed by sorting ordinals in ``_add_dissolve``.
- 2026-04-25 — Bin twin id collision fix: renamed suffix `_bin` → `_kdbin` so a producer named `main` doesn't collide with the reserved `<playlist id="main_bin">` infrastructure element. Crash repro was smoke 022 (PIP).
- 2026-04-25 — Smoke 7 (timeline editing): split, trim, move, ripple_delete all clean via the existing patcher path. PIP works through the `EntryFilter` qtblend pattern. Documented that `track_mute`, `track_visibility`, `audio_fade`, and `composite_set` blend modes still emit opaque-fragment XML at root and need serializer rework.
- 2026-04-25 — Clip speed (timewarp) pattern: reference fixture `clip_speed_400_native.kdenlive`. Added `PlaylistEntry.speed` field; serializer emits a `<producer mlt_service="timewarp">` per unique (producer, speed) pair and rewrites entry refs to point at the variant. Original chain stays as the bin clip. Fixed `_apply_set_clip_speed` to set `entry.speed` and rescale `out_point` instead of the broken opaque-XML form. See [[kdenlive-clip-speed-pattern]].
- 2026-04-25 — Smoke batches 5+6: image producers (`mlt_service=qimage`) and Ken Burns / parallax via `qtblend` transform filters. Reference: ``04-image transform.kdenlive``. Critical findings -- `clip_type=2` (not 5) for image producers, `length`/`kdenlive:duration` in timecode, `ttl=25` + `format=1` required, image producers carry `kdenlive:kextractor=1` (single-instance bin marker, no twin), transforms use `qtblend` (not `affine`), filter goes *inside* the `<entry>` element, keyframe timestamps are entry-local (not absolute sequence frames). Added `EntryFilter` model + `PlaylistEntry.filters` and full parallax smoke test 017. See [[kdenlive-image-and-qtblend-pattern]].
